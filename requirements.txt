# Core vision
ultralytics>=8.3.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# PyTorch — CPU build (smaller, works everywhere; swap for GPU build if needed)
# GPU: remove --extra-index-url line and use: torch>=2.0.0+cu118
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# Re-ID model (DINOv2 via HuggingFace)
transformers>=4.35.0

# Clustering for post-hoc deduplication
scipy>=1.10.0
scikit-learn>=1.3.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# AWS
boto3>=1.28.0

# HTTP download
requests>=2.31.0
